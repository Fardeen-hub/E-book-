<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>E-Book Library</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    /* Global Styles */
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: 'Poppins', sans-serif;
      overflow: hidden;
    }

    /* Background Styles (Image or Video) */
    .background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* Video background */
    .video-background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -1;
    }

    /* Home Container */
    .home-container {
      position: relative;
      z-index: 1;
      text-align: center;
      color: white;
      padding: 50px;
      max-width: 800px;
      margin: 0 auto;
    }

    h1 {
      font-size: 60px;
      font-weight: 700;
      margin-bottom: 20px;
      font-family: 'Playfair Display', serif;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
    }

    p {
      font-size: 20px;
      font-weight: 400;
      margin-bottom: 40px;
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
    }

    /* Button Container */
    .button-container {
      display: flex;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
    }

    .button-container a {
      text-decoration: none;
    }

    /* Button Styles */
    .btn {
      background-color: rgba(0, 0, 0, 0.7);
      color: white;
      padding: 15px 30px;
      font-size: 18px;
      font-weight: 500;
      border-radius: 30px;
      border: 2px solid rgba(255, 255, 255, 0.5);
      transition: all 0.3s ease;
      width: 220px;
      text-transform: uppercase;
    }

    .btn:hover {
      background-color: rgba(0, 0, 0, 0.9);
      border-color: white;
      transform: translateY(-5px);
    }

    .btn-register {
      background-color: #2196F3;
    }

    .btn-register:hover {
      background-color: #0b7dda;
    }

    .btn-explore {
      background-color: #FF6F61;
    }

    .btn-explore:hover {
      background-color: #d45544;
    }

    footer {
      position: fixed;
      bottom: 20px;
      font-size: 14px;
      color: white;
      text-align: center;
      z-index: 1;
    }

    footer a {
      color: #2196F3;
      text-decoration: none;
    }

  </style>
</head>
<body>

  <!-- Background Video or Image -->
  <video autoplay muted loop class="video-background">
    <source src="/bg.mp4" type="video/mp4">
    Your browser does not support the video tag.
  </video>

  <!-- Home Page Container -->
  <div class="home-container">
    <h1>Welcome to the E-Book Library</h1>
    <p>Discover a world of knowledge, stories, and inspiration with our digital library of e-books.</p>

    <!-- Button Container -->
    <div class="button-container">
      <a href="/login"><button class="btn">Login</button></a>
      <a href="/register"><button class="btn btn-register">Register</button></a>
      <a href="/books/show"><button class="btn btn-explore">Explore E-Books</button></a>
    </div>
  </div>



</body>
</html>
